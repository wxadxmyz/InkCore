# 墨核 AI Studio · 部署步骤（已填真实用户名，照做即可）

> 仓库地址：`https://github.com/wxadxmyz/InkCore`
> 本文是「可直接照抄执行」的精简版；完整说明（含排错、截图级指引）见 `GITHUB_DEPLOY.md`。
> 所有 `git` 命令在你**自己的电脑终端**执行（当前沙箱连不上 github.com）。

---

## A. 在 GitHub 网页上做（一次性）

1. 登录 https://github.com → 右上角头像 → **Your repositories** → 绿色 **New**。
2. 填写：
   - **Repository name**：`InkCore`
   - **Public**（公开，Actions 免费）
   - ⚠️ **不要勾** Add a README file / Add .gitignore / Choose a license（本地已有，避免冲突）
3. 点 **Create repository**。
4. 点绿色 **Code** → 复制 HTTPS 地址（形如 `https://github.com/wxadxmyz/InkCore.git`，记下即可，下面已填好）。

---

## B. 在自己电脑终端执行（一次性）

5. 进入项目目录（换成你的真实路径）：
   ```bash
   cd D:\InkCore      # Windows 举例
   # 或  cd ~/InkCore  # macOS / Linux 举例
   ```

6. 关联远程仓库：
   ```bash
   git remote add origin https://github.com/wxadxmyz/InkCore.git
   ```

7. 分支对齐（本地是 `master`，GitHub 默认 `main`）并首次推送：
   ```bash
   git branch -M main
   git push -u origin main
   ```
   > 推送时会要账号密码：**用户名**填 GitHub 账号，**密码**填 Personal Access Token（见下方「取 Token」）。GitHub 已停用登录密码。

8. 刷新 `https://github.com/wxadxmyz/InkCore` 确认文件已上传（应看到 `app.py`、`static/`、`build.spec`、`.github/` 等约 90 个文件）。

---

## C. 打 tag 触发自动打包（发正式版本时做）

9. 打版本标签并推送（当前版本 **v1.0.0**，见 `version.json`）：
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```

10. 打开仓库 **Actions** 标签，看 `Build Windows Release` 工作流跑完（首次约 5–15 分钟，全绿对勾即成功）。

11. 进仓库右侧 **Releases** → 展开 Assets 下载：
    - `InkCore.exe` —— 单文件版，双击即用
    - `InkCore_Setup_1.0.0.exe` —— 安装版（推荐）
    > 首次运行若见 SmartScreen 提示，点「详细信息 → 仍要运行」（未签名程序正常提示，非病毒）。

---

## D. 日常更新代码（每次改完都这样）

```bash
git add -A
git commit -m "这里写改了什么"
git push
```
> 这一步只更新源码，**不会**自动打包。

发新版本时（先改 `version.json` 里的 `version` 和 `url` 再提交）：
```bash
git tag v1.1.1
git push origin v1.1.1
```

---

## 取 Personal Access Token（首次推送要用）

1. github.com → 头像 → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)** → **Generate new token**。
2. Note 随便填（如 `InkCore-deploy`）；Expiration 选 `90 days` 或 `No expiration`；**Select scopes 勾选 `repo`**。
3. 拉到底 **Generate token** → 复制页面上 `ghp_xxxxxxxx` 那一串（离开就看不到了）。
4. 推送时「密码」一栏粘贴这串 token。

> 想以后免密：配置 SSH key 后执行 `git remote set-url origin git@github.com:wxadxmyz/InkCore.git`。

---

## 完成标志 ✅

GitHub 仓库 `Releases` 中出现 `墨核 AI Studio v1.0.0`，Assets 含：
- `InkCore.exe`
- `InkCore_Setup_1.0.0.exe`

---

## 一页速查（复制整段即可走完主流程）

```bash
# 在自己电脑、进入项目目录后执行
git remote add origin https://github.com/wxadxmyz/InkCore.git
git branch -M main
git push -u origin main
git tag v1.0.0
git push origin v1.0.0
# 然后去 GitHub：Actions 看进度 → Releases 下载 exe
```
