flask>=3.0
python-docx>=1.0
pywebview>=6.0
requests>=2.28
beautifulsoup4>=4.12        # 联网搜索（DuckDuckGo HTML 解析）
reportlab>=4.0             # PDF 导出
pillow>=10.0               # 系统托盘图标
pystray>=0.19              # 系统托盘菜单
keyboard>=0.13             # 全局热键（Windows）
llama-cpp-python>=0.2.0    # 本地内置模型（离线推理，免 Ollama）；打包时把原生推理库打进 exe
# 仅打包时需要：pyinstaller
pyinstaller>=6.0
